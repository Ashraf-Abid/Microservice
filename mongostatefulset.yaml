apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo
spec:
  serviceName: 'mongoservice'
  replicas: 1
  selector:
    matchLabels:
      app: mongodbpodlebel
  template:
    metadata:
      labels:
        app: mongodbpodlebel
    spec:
      containers:
      - image: mongo
        imagePullPolicy: Always
        volumeMounts:
          - mountPath: /data/db
            name: abc
        name: mongo
        ports:
          - containerPort: 27017
        envFrom:
          - secretRef:
             name: mysecret
  volumeClaimTemplates:
    - metadata:
        name: abc
      spec:
        accessModes: ['ReadWriteOnce']
        resources:
          requests:
            storage: 2Gi